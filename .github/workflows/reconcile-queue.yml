name: Reconcile self-sustaining queue (frozen)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Explain frozen legacy workflow
        run: |
          echo "::error::Legacy task/queue reconciliation is frozen while PR #139 replaces the control plane."
          echo "This workflow must not create campaign issues, materialize tasks, or push directly to main."
          exit 1
